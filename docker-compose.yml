name: xray_docker_tls
services:
    xray-docker-tls:
        container_name: xray_tls
        ports:
            - 80:80
            - 443:443
        restart: unless-stopped
        image: 22ee0277/xray-docker-tls
